{
  "name": "aprs-is-demo",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "setup": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node dist/app",
    "tsc": "./node_modules/.bin/tsc"
  },
  "author": "afairhurst",
  "license": "MIT",
  "dependencies": {
    "js-aprs-fap": "^2.0.0",
    "js-aprs-is": "^1.0.4"
  },
  "devDependencies": {
    "typescript": "^4.0.2"
  }
}
